version: '3.4' 

services:
  firstwebapplication:
    image: ${DOCKER_REGISTRY-}firstwebapplication
    build:
      context: .
      dockerfile: FirstWebApplication/Dockerfile
    ports:
      - "5000:8080"
 
    environment:
      - ASPNETCORE_ENVIROMENT=development
      - ConnectionStrings__DefaultConnection=server=mariadbcontainer;port=3306;database=mysql;user=root;password=haterPassord00;
            
    networks:
      - appnet
networks:
     appnet:
      external: true
      name: appnet