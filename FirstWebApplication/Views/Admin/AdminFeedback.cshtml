@model IEnumerable<FirstWebApplication.Models.Advice>

@{
    ViewData["Title"] = "Tilbakemeldinger";
}

<h1>Tilbakemelding</h1>

<table class="table">
    <thead>
    <tr>
        <th>Bruker</th>
        <th>Melding</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var f in Model)
    {
        <tr>
            <td>@f.adviceID</td>           
            <td>@f.adviceMessage</td>    
        </tr>
    }
    </tbody>
</table>